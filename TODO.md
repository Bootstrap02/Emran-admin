# TODO: Add Elections route to Information Management

## Steps
1. [x] App.js — Import `CreateElection` and `AdminManageCandidates` from `./Pages/Createinfo`
2. [x] App.js — Add routes `/elections/create/:id` and `/elections/manage`
3. [x] Header.js — Add "Create Election" and "Manage Elections" items to "Information MGt" dropdown
4. [x] Verify build/runtime
